1、设置动态库加载命令，启动server之前先执行命令
export LD_LIBRARY_PATH=./
或者也可以这样
vim /etc/ld.so.conf 
文件中加入一行  /home/luotan/linuxserverplatform/bin/
执行 root权限运行 ldconfig

2、启动程序必须用shell脚本启动，方便生产core文件
注意关闭会话无效 ulimit -c unlimited